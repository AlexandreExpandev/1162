import { Router } from 'express';

const router = Router();

// Internal authenticated routes will be added here as features are implemented
// Example: router.use('/timer', timerRoutes);

export default router;
