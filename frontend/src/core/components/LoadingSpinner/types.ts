/**
 * @interface LoadingSpinnerProps
 * @summary Type definitions for LoadingSpinner component properties.
 */
export interface LoadingSpinnerProps {
  /**
   * Size of the spinner
   * @default 'medium'
   */
  size?: 'small' | 'medium' | 'large';

  /**
   * Additional CSS classes
   */
  className?: string;
}
